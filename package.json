{
    "name": "slack-chat",
    "version": "1.0.0",
    "main": "index.js",
    "license": "MIT",
    "scripts": {
        "dev": "(nodemon app.js) & (cd client && yarn serve)",
        "install-all":"yarn install & (cd client && yarn install)"
    },
    "dependencies": {
        "@slack/events-api": "^2.3.0",
        "@slack/rtm-api": "^5.0.3",
        "@slack/web-api": "^5.6.0",
        "dotenv": "^8.2.0",
        "express": "^4.17.1",
        "global": "^4.4.0",
        "nodemon": "^2.0.1",
        "request": "^2.88.0",
        "socket.io": "^2.3.0"
    }
}
